package Project2;

import java.util.Arrays;
import java.util.List;

public class ArryListStartA{
    public static void main(String[] args) {
        List<String> stringList = Arrays.asList("Apple", "Banana", "Orange", "Apricot");
        //List<String> filteredList = stringList.stream();
           //     .filter(s -> s.startsWith("A"))
            //    .map(String::toLowerCase)
             //   .toList();
       // System.out.println("Filtered Strings starting with 'A': " + filteredList);
    }


}